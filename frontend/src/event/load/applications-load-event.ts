import {LoadEvent} from './load-event';

/*
 * Event for requesting all applications to be loaded.
 */
export class ApplicationsLoadEvent extends LoadEvent {
  // TODO: add filtering later for loaded applications
}

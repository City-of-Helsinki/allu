// Minimal type declarations for the `file-saver` package.
// The npm package `file-saver` does not ship its own types.

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const saveAs: any;

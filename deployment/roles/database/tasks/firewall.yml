
- name: allow Docker container access to database in test environment
  ufw: rule=allow port={{ item }} src=172.17.0.1/24
  when: "'test' in group_names"
  become: yes
  with_items:
    - 5432 # database

# TODO: add firewall rule for accessing database from local network to support deployment of database to separate server
